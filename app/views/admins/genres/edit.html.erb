<%= form_for(@genre, url:admins_genre_path) do |f| %>

<%= f.label "ジャンル" %>
<%= f.text_area :name %>

<%= f.submit %>
<%= link_to "削除", admins_genre_path(@genre), method: :delete, data: {confirm: "本当に削除しますか？"} %>

<% end %>