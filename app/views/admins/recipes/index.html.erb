<% @recipes.each do |recipe| %>

<%= attachment_image_tag recipe, :recipe_image  %>
<%= attachment_image_tag recipe.user, :profile_image, fallback: "no-image.png", :size =>'100x100' %>


<%= recipe.user.name %>
<%= recipe.name %>

<%= link_to "削除",admins_recipe_path(recipe), method: :delete, data: {confirm: "本当に削除しますか？"} %>
admins_recipe_path

<% end %>