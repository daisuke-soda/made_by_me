<div class="container">
  <div class="row">
    <div class = "col-9">
      <h2>レシピ一覧</h2>
    </div>
</div>
</div>
<% @search_recipes.each do |recipe| %>

<%= link_to recipe_path(recipe) do %>
<%= attachment_image_tag recipe, :recipe_image, :size =>'200x200' %>
<% end %>
<%= attachment_image_tag recipe.user, :profile_image, fallback: "no-image.png", :size =>'100x100' %>


<%= recipe.user.name %>
<%= recipe.name %>

<% end %>

